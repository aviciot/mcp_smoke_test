# Environment Variables
# =====================

# Environment (dev, prod, or default)
ENV=default

# ========================================
# MCP SERVER CONFIGURATION
# ========================================

# Container and service name
MCP_CONTAINER_NAME=template_mcp
MCP_NAME=template-mcp

# Server port (internal)
MCP_PORT=8100

# Auto-Discovery (automatically load all tools/resources/prompts)
AUTO_DISCOVER=true

# ========================================
# AUTHENTICATION
# ========================================
AUTH_ENABLED=false
AUTH_TOKEN=your-secret-token-here

# ========================================
# LOGGING
# ========================================
LOG_LEVEL=INFO
LOG_FORMAT=text  # text or json

# ========================================
# TRAEFIK GATEWAY CONFIGURATION
# ========================================
# Set to 'true' to enable Traefik routing
# When enabled:
# 1. Comment out 'ports:' in docker-compose.yml
# 2. Uncomment Traefik labels in docker-compose.yml
# 3. Uncomment mcp_network in docker-compose.yml
USE_TRAEFIK=false

# Traefik entry point (usually 'web' for HTTP)
TRAEFIK_ENTRYPOINT=web

# Path prefix for this MCP (e.g., /template-mcp)
# The gateway will route http://gateway:8000/template-mcp → this MCP
TRAEFIK_PATH_PREFIX=/${MCP_NAME}

# ========================================
# CUSTOM ENVIRONMENT VARIABLES
# ========================================
# Add your application-specific variables below
# Examples:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# API_KEY=your_api_key_here
# EXTERNAL_SERVICE_URL=https://api.example.com
